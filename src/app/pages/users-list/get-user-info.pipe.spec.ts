import {GetUserInfoPipe} from './get-user-info.pipe';

describe('GetUserInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetUserInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
